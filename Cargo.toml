[package]
name = "dnd_stats"
version = "0.1.0"
edition = "2024"

# Set opt-level = 3 in dev so plain `cargo build/run/test` is always fast.
[profile.dev]
opt-level = 3

[dependencies]
rand = "0.10.0"
clap = { version = "4", features = ["derive"] }
yansi = "1"
